export type {
  AppendOptions,
  AppendResult,
  DurableStreamStore,
  MaybePromise,
  ProducerState,
  ProducerValidationResult,
  Stream,
  StreamMessage,
} from "@durable-streams/server/types";

export type Offset = string;
