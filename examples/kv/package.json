{
  "name": "kv",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "test": "vitest run",
    "types": "wrangler types"
  },
  "devDependencies": {
    "@durable-streams/server-conformance-tests": "^0.1.8",
    "vitest": "^4.0.17",
    "wrangler": "^4.58.0"
  },
  "dependencies": {
    "durable-cf-streams": "workspace:*"
  }
}
